import { VehicleUpgradeStatements } from './vehicle.upgrade.statements';

describe('VehicleUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new VehicleUpgradeStatements()).toBeTruthy();
  });
});
